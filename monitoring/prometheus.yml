global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'llm-gateway'
    authorization:
      type: Bearer
      credentials_file: /etc/prometheus/metrics_token
    static_configs:
      - targets: ['backend:8000']